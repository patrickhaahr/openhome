Implement ticket 05 — Compact Timeline (read-only).

Work in /home/hermes/dev/openhome. Scope: mobile-expo/ ONLY. No API/backend changes, no changes to mobile/ (deprecated), no edits outside mobile-expo/ except reading. The attached ORCHESTRATION.md documents conventions and the validation matrix; spec.md and CONTEXT.md are the product source of truth; the attached ticket file is the acceptance checklist. Ticket 01 is merged: the Server Tab shell, refresh conventions, and shared UI primitives exist — build on them, do not restructure them.

Hard requirements from the spec you must preserve:
- Server Tab gains the Compact Timeline: newest-first Feed items from `GET /api/timeline?view=compact&limit&before_id` (compact view only), rendered compactly — it is a skim list, not an article reader.
- Reaching the end of the list loads the next page via `before_id` (last item's id); no duplicates across pages, correct newest-first ordering, and stale-response guarding (a slow earlier page must not clobber a newer one).
- Tapping an item opens its link in the system browser via React Native `Linking.openURL`.
- Load-more failures surface inline and do NOT drop already-rendered items; retry resumes pagination at the correct position.
- Empty timeline shows an empty state, distinct from an error.
- Extend the single shared Axum adapter with an rss method group (compact timeline endpoint) keeping the short 5s timeout; response-shape guards reject malformed payloads — compact-timeline array validation is explicitly in scope per spec Testing Decisions S2.
- One application reducer state machine hook (typed event union, use-adguard.ts pattern) owning initial load, pull-to-refresh, foreground refocus, and pagination. Inline loading/error states per surface; palette tokens only.

Testing per spec Testing Decisions: machine tests against the scripted fake adapter (S1) covering initial load, pagination, stale-response guarding, and failure paths; compact-timeline response parsing through stubbed global fetch (S2). Colocate tests next to sources.

Validation — run exactly this from the repo root and make it pass before you finish (bun is only available via the PATH prefix shown; do not skip it):
nix develop -c bash -c 'export PATH=/tmp/oh-bin:$PATH && just expo-typecheck && just expo-test && just expo-lint'

Do NOT run: git commit, git add, just expo-check, just expo-android. A human reviewer commits later.

Follow the /implement command you were invoked with: implement with /tdd at the pre-agreed seams, then run the reviewer subagent on the diff with the acceptance criteria, fix blocker/high/medium findings (resume the reviewer once with only the fixes, max three reviewer calls total), then run simplifier once as advisory only. Finish by reporting: files changed, validation output summary, reviewer findings and their resolution, and the commit message you would use (do not commit).
