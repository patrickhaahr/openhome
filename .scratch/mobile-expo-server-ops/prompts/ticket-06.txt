Implement ticket 06 — Feed manager.

Work in /home/hermes/dev/openhome. Scope: mobile-expo/ ONLY. No API/backend changes, no changes to mobile/ (deprecated), no edits outside mobile-expo/ except reading. The attached ORCHESTRATION.md documents conventions and the validation matrix; spec.md and CONTEXT.md are the product source of truth; the attached ticket file is the acceptance checklist. Tickets 01 and 05 are merged: the Server Tab, refresh conventions, and the Compact Timeline state machine exist — build on them, do not restructure them.

Hard requirements from the spec you must preserve:
- An inline COLLAPSIBLE "manage feeds" section on the Server Tab (no Modal component introduced) listing current Feeds with delete buttons.
- Add a Feed by pasting a URL; INVALID URLs are rejected client-side with a clear message BEFORE any request is sent.
- Adding a valid URL creates the Feed (`POST /api/feeds`) and refreshes BOTH the feed list and the Compact Timeline.
- Delete (`DELETE /api/feeds/:id`) removes the Feed immediately and refreshes the timeline; deleting a Feed removes its items from the Compact Timeline.
- Undo re-adds the deleted Feeds as a BATCH (undo restores all Feeds deleted since the last undo/creation, re-add via `POST /api/feeds`) and refreshes the timeline. Per spec: undo re-adds deleted Feeds, plural — batch it.
- Create/delete failures surface inline WITHOUT losing the entered URL.
- Extend the rss adapter group (list/create/delete feeds) with response-shape guards; short 5s timeout; extend the rss state machine (typed event union) keeping timeline pagination, pull-to-refresh, and foreground-refocus intact. Palette tokens only.

Testing per spec Testing Decisions: machine tests against the scripted fake adapter (S1) covering add, delete, batched undo, invalid-URL rejection, and failure paths (including URL preservation on failure). Colocate tests next to sources.

Validation — run exactly this from the repo root and make it pass before you finish (bun is only available via the PATH prefix shown; do not skip it):
nix develop -c bash -c 'export PATH=/tmp/oh-bin:$PATH && just expo-typecheck && just expo-test && just expo-lint'

Do NOT run: git commit, git add, just expo-check, just expo-android. A human reviewer commits later.

Follow the /implement command you were invoked with: implement with /tdd at the pre-agreed seams, then run the reviewer subagent on the diff with the acceptance criteria, fix blocker/high/medium findings (resume the reviewer once with only the fixes, max three reviewer calls total), then run simplifier once as advisory only. Finish by reporting: files changed, validation output summary, reviewer findings and their resolution, and the commit message you would use (do not commit).
