Implement ticket 02 — Container list + Docker Health Summary.

Work in /home/hermes/dev/openhome. Scope: mobile-expo/ ONLY. No API/backend changes, no changes to mobile/ (deprecated), no edits outside mobile-expo/ except reading. The attached ORCHESTRATION.md documents conventions, the validation matrix, and per-ticket rules from ticket 01; the attached spec.md and CONTEXT.md are the product source of truth; the attached ticket file is the acceptance checklist.

Hard requirements from the spec you must preserve:
- New Docker Tab as a Top-Level Tab appended after Server in the existing custom pager (Home, TV, Speaker, Away, Server, Docker). No router dependency.
- Extend the single shared Axum API adapter (src/infrastructure/open-home-api.ts) with a docker method group. Docker requests use the extended ~15s timeout; all other adapter calls keep the short 5s one. Response-shape guards reject malformed payloads as failures. Container parsing tolerates BOTH PascalCase and snake_case field spellings (inherited wire contract) and normalizes numeric uptime given as a string.
- One application reducer state machine hook (typed event union) following the existing use-adguard.ts pattern. It owns: initial load, pull-to-refresh, foreground-refocus refetch (existing AppState resume point), and exposes per-classification counts.
- Docker Health Summary card on the Server Tab classifies: healthy / unhealthy / idle (nothing running) / offline (Axum API unreachable — a distinct state, not idle), shows N-of-M running, and tapping it switches to the Docker Tab.
- Docker Tab lists containers with name, image, run state, health status, uptime, ports, restart count; health filter chips (all/healthy/unhealthy/stopped) with per-classification counts; loading and error states inline per surface.
- Status semantics map onto existing palette tokens (ready/signal/danger); no theme additions.

Testing per spec Testing Decisions: machine tests against a scripted fake of the adapter interface (S1); container-list parsing tests through stubbed global fetch (S2) covering case fallbacks and string uptime. Colocate tests next to sources, following the use-adguard.test.ts / open-home-api.test.ts patterns.

Validation — run exactly this from the repo root and make it pass before you finish (bun is only available via the PATH prefix shown; do not skip it):
nix develop -c bash -c 'export PATH=/tmp/oh-bin:$PATH && just expo-typecheck && just expo-test && just expo-lint'

Do NOT run: git commit, git add, just expo-check, just expo-android. A human reviewer commits later.

Follow the /implement command you were invoked with: implement with /tdd at the pre-agreed seams (adapter parsing guard, machine), then run the reviewer subagent on the diff with the acceptance criteria, fix blocker/high/medium findings (resume the reviewer once with only the fixes, max three reviewer calls total), then run simplifier once as advisory only. Finish by reporting: files changed, validation output summary, reviewer findings and their resolution, and the commit message you would use (do not commit).
