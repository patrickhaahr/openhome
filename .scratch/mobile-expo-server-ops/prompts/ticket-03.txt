Implement ticket 03 — Container lifecycle actions.

Work in /home/hermes/dev/openhome. Scope: mobile-expo/ ONLY. No API/backend changes, no changes to mobile/ (deprecated), no edits outside mobile-expo/ except reading. The attached ORCHESTRATION.md documents conventions and the validation matrix; spec.md and CONTEXT.md are the product source of truth; the attached ticket file is the acceptance checklist. Ticket 02 is already merged: the Docker Tab, container list, docker adapter group, and the docker state machine exist — build on them, do not restructure them.

Hard requirements from the spec you must preserve:
- Each Container row offers start, stop, and restart that execute IMMEDIATELY — no confirmation dialog of any kind (deliberate single-user homelab choice).
- Successful actions update the affected Container's state in the list (refresh after dispatch).
- Failures are surfaced inline and distinguished by class: 404 renders a "container not found" message naming the Container; 503 renders "Docker unavailable"; other failures render their error text inline without crashing the list.
- Extend the existing docker adapter group (src/infrastructure/open-home-api.ts) with start/stop/restart keeping the extended docker timeout (~15s) and Result-style failures; response-shape guards reject malformed payloads.
- Extend the existing docker state machine (typed event union, use-adguard.ts pattern) with immediate lifecycle dispatch; keep pull-to-refresh and foreground-refocus behavior intact.
- Map errors onto existing palette tokens (ready/signal/danger); no theme additions.

Testing per spec Testing Decisions: machine tests against the scripted fake adapter (S1) covering immediate dispatch, success refresh, and each distinct failure class (not found / unavailable / other). If adapter error-body extraction needs new parsing logic, cover it through stubbed global fetch (S2). Colocate tests next to sources.

Validation — run exactly this from the repo root and make it pass before you finish (bun is only available via the PATH prefix shown; do not skip it):
nix develop -c bash -c 'export PATH=/tmp/oh-bin:$PATH && just expo-typecheck && just expo-test && just expo-lint'

Do NOT run: git commit, git add, just expo-check, just expo-android. A human reviewer commits later.

Follow the /implement command you were invoked with: implement with /tdd at the pre-agreed seams (adapter error extraction, machine lifecycle events), then run the reviewer subagent on the diff with the acceptance criteria, fix blocker/high/medium findings (resume the reviewer once with only the fixes, max three reviewer calls total), then run simplifier once as advisory only. Finish by reporting: files changed, validation output summary, reviewer findings and their resolution, and the commit message you would use (do not commit).
