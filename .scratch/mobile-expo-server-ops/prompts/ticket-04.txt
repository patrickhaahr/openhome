Implement ticket 04 — Container logs.

Work in /home/hermes/dev/openhome. Scope: mobile-expo/ ONLY. No API/backend changes, no changes to mobile/ (deprecated), no edits outside mobile-expo/ except reading. The attached ORCHESTRATION.md documents conventions and the validation matrix; spec.md and CONTEXT.md are the product source of truth; the attached ticket file is the acceptance checklist. Tickets 02–03 are already merged: the Docker Tab, container list, docker adapter group, and docker state machine exist — build on them, do not restructure them.

Hard requirements from the spec you must preserve:
- Tapping a Container on the Docker Tab swaps the list view for that Container's logs (rendered view swap inside the tab, NOT a Modal and NOT a new Top-Level Tab); back returns to the unfiltered container list without stale log state.
- Log fetch requests the FIXED tail of 200 lines with timestamps=true and passes NO `since` filter. One-shot fetch + manual refresh only — no auto-scroll toggle, no live tailing, no periodic polling.
- Not-found (404) renders "container not found" naming the container; other failures render inline per the existing error-class pattern from ticket 03.
- Extend the existing docker adapter group with a logs method on the extended docker timeout (~15s); response-shape guards reject malformed payloads as failures.
- Machine owns: selected-container state, log load, manual refresh, back/reset. Keep pull-to-refresh and foreground-refocus behavior for the list intact.
- Map states onto existing palette tokens; no theme additions.

Testing per spec Testing Decisions: machine tests against the scripted fake adapter (S1) covering load, refresh, back-reset, and failure paths; any new wire parsing (e.g. logs payload shape) through stubbed global fetch (S2). Colocate tests next to sources.

Validation — run exactly this from the repo root and make it pass before you finish (bun is only available via the PATH prefix shown; do not skip it):
nix develop -c bash -c 'export PATH=/tmp/oh-bin:$PATH && just expo-typecheck && just expo-test && just expo-lint'

Do NOT run: git commit, git add, just expo-check, just expo-android. A human reviewer commits later.

Follow the /implement command you were invoked with: implement with /tdd at the pre-agreed seams, then run the reviewer subagent on the diff with the acceptance criteria, fix blocker/high/medium findings (resume the reviewer once with only the fixes, max three reviewer calls total), then run simplifier once as advisory only. Finish by reporting: files changed, validation output summary, reviewer findings and their resolution, and the commit message you would use (do not commit).
